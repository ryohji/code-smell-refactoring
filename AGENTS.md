# エージェント向け指示：コード改善作業の手順

リファクタリング・コードの臭いの分析・テストのないコードへのテスト追加を依頼されたら、**作業を始める前に次を読む。**

1. `code-smell-refactoring/SKILL.md` — 全フェーズの流れ
2. `code-smell-refactoring/references/capabilities.md` — 自分の実行方式（層 1／層 2）の判定

該当する依頼の例。

- 「このモジュールをリファクタリングして」「コードの臭いを分析して」
- 「レガシーコードを改善したい」「技術的負債を返済したい」
- 「テストがないコードを直したい」「この関数にテストを追加して」
- 「この関数が長すぎる／読みにくいので整理して」
- 具体的な手法名の指定（メソッド抽出、ガード節導入、依存逆転、クラス分割など）

## 手順を読む前に頭に入れておくこと

この作業には、通常のコード修正と違う 3 つの規律がある。

1. **テストのないコードは触らない。** まずテストで保護し、そのあとで書きかえる。保護なしの書きかえは、壊したかどうかを確認できないので作業として成立しない。
2. **小さなステップでグリーンを保つ。** 1 コミット 1 変更。各コミット後にテストを実行する。レッドになったら直そうとせず直前のコミットに戻す。
3. **テストの assertion を変えない。** リファクタリングとは外部から見たふるまいを変えずに内部構造を改善すること。期待値を変えたくなったら、それはふるまいを変えている証拠なので、実装を戻してユーザーに報告する。

もうひとつ、実務上の要点がある。**進捗は `REFACTORING_PLAN.md` に書く。** 会話履歴を進捗の記録にしない。長い作業なので文脈は必ず限界に近づくが、このファイルがあればどこからでも再開できる。1 項目終わるたびに更新する。

## サブタスクを起動できない場合

多くのツールは単一の会話で動く（層 2）。その場合も手順は変わらないが、**作業をステップ単位で区切り、区切りごとに文脈を捨てる**。

- 1 セッション 1 ステップ。ステップ A（テストで保護）が終わったらセッションを終える
- セッションの冒頭で `REFACTORING_PLAN.md` を読む。これが引き継ぎ書
- セッションの終わりに必ず計画ファイルを更新する
- **ステップ C（検証）は必ず新しいセッションで行う。** 同じ文脈のまま検証すると、自分の変更を「正しいはず」という前提で見てしまう

詳細は `references/capabilities.md` の「層 2」を読む。
