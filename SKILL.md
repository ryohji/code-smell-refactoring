---
name: code-smell-refactoring
description: コードの臭い（重複コード、長すぎるメソッド、肥大化クラス、不適切な責務配置、不明瞭な名前、複雑な条件分岐、強すぎる依存、データの散在、不要なコード、理解しづらいロジック）を検出して優先度づけし、テストで保護しながら小さなステップでリファクタリングする。「このモジュールをリファクタリングして」「コードの臭いを分析して」「レガシーコードを改善したい」「テストがないコードを直したい」「技術的負債を返済したい」「この関数が長すぎる／読みにくいので整理して」といった依頼で必ず使う。特定のリファクタリング名（メソッド抽出、ガード節導入、依存逆転など）を挙げられた場合、テストのないコードに単体テストを追加する場合、既存実装を壊さずに設計を変えたい場合にも使う。
---

# コードの臭いにもとづく段階的リファクタリング

『ソフトウェア実装改善ガイド』の方法論を実行する。中核は次の 3 点。

1. **テストのないコードは触らない。** まずテストで保護し、そのあとで書きかえる。
2. **小さなステップでグリーンを保つ。** 各ステップ後にテストを実行し、レッドになったら即座に戻す。
3. **テストは変えない。** リファクタリングとは「外部から見たふるまいを変えずに内部構造を改善すること」。テストを書きかえたくなったら、それはリファクタリングではなく仕様変更。

## 最初にやること：実行方式を決める

**着手前に `references/capabilities.md` を読む。** このスキルは特定のツールに依存しないが、サブタスクを起動できるかどうかで進め方が変わる。

- **層 1**（サブタスクを起動できる）：検出・保護・修正・検証を別々のサブタスクに委譲する
- **層 2**（単一の会話のみ）：作業をステップ単位で区切り、区切りごとに文脈を捨てる

判断がつかなければ**層 2 として振るまう**。層 2 の手順は層 1 でも正しく動く。

## 共通の原則：作業役と司令役を分ける

どちらの層でも、**司令役は実装コードをほとんど読まない**。

長いソースを読むと文脈がすぐ埋まり、方針や進捗を見失う。だから司令役は台帳の管理と判断に徹し、コードを読む作業・テストを書く作業・修正する作業は作業役が担う。層 1 では作業役が別のサブタスク、層 2 では**文脈を切ったあとの自分**になる。

司令役が保持するのは次だけ。

- 対象モジュールのファイル一覧とテスト実行コマンド
- 臭いの一覧と優先度（構造化された台帳）
- 各項目の状態（未着手／保護済み／修正済み／棚上げ）
- 作業役から返ってきた要約（各 20 行以内）

進捗台帳は会話ではなくファイル（`REFACTORING_PLAN.md`）に置く。会話が圧縮されても、あるいはセッションが変わっても、ファイルを読みなおせば再開できる。**このファイルが唯一の正本。**

## 手順

| フェーズ | 内容 | 詳細 |
|---|---|---|
| 0 | 環境の確認（テストが動くか） | 下記「フェーズ 0」 |
| 1 | 臭いの検出と優先度づけ | `references/smells.md` |
| 2 | 計画の記録と合意 | 下記「フェーズ 2」 |
| 3 | 1 項目ずつ修正（保護 → 変更 → 検証） | `references/refactoring-loop.md` |
| 4 | 仕上げ（仕様の起こしなおし、報告） | 下記「フェーズ 4」 |

各ステップの指示文はすべて `references/task-prompts.md` にある。層 1 ではサブタスクへ渡す文、層 2 では自分が守る規則として読む。フェーズ 1 と 3 に入る前に開く。

## フェーズ 0：足場をつくる

テストが動かない環境でリファクタリングを始めると、失敗の原因が「自分の変更」か「環境」か切り分けられなくなる。だから最初に、テストフレームワークが正しく動くことを確かめる。

1. **テスト実行コマンドを特定する。** ビルド設定（`pytest.ini`, `pom.xml`, `CMakeLists.txt`, `package.json`, `Makefile` など）を見て、テストを 1 コマンドで実行する方法を決める。以後このコマンドを全ステップで使う。
2. **現状のグリーンを確認する。** 実行して、全テストが通ることを確かめる。**すでにレッドなら、そこで止まってユーザーに報告する。** 壊れているテストがある状態では「自分が壊したか」を判定できない。
3. **テストが存在しない場合は、テストフレームワークの疎通を確認する。** 空のテストを 1 つ作って実行し OK を得る。次に必ず失敗する assertion（`assert False` 相当）を入れて、レッドが正しく報告されることを確認する。確認後にその失敗テストを削除する。この「レッドが出ることの確認」を省くと、テストが実は 1 件も収集されていないのに「グリーン」と誤認する事故が起きる。

疎通確認は `scripts/verify_harness.sh` が代行できる（言語を自動判定してコマンド候補を提示する）。

このフェーズは司令役が自分で実行してよい。出力が短く、以後の全作業の前提になる情報だからだ。

## フェーズ 1：臭いを検出する

`references/smells.md` を読んでから実行する。臭いの 10 分類、優先度の付け方、検出後の裏取りが書かれている。

指示文は `references/task-prompts.md` の「臭いの検出」。**返るのは構造化された表だけ**という制約が要点で、対象が数万行あっても台帳は数十行で済む。

- **層 1**：対象を 2〜4 個のサブタスクに分割して並行調査させる。担当を分けると視点が分かれて検出漏れが減る
- **層 2**：対象を 2〜4 区画に分け、**1 セッション 1 区画**で調べる。各セッションの終わりに、見つけた臭いを表の行として `REFACTORING_PLAN.md` に追記してセッションを終える。読んだコードは会話に残さず捨てる

## フェーズ 2：計画を記録して合意する

フェーズ 1 で集めた臭いを、対象リポジトリのルートに `REFACTORING_PLAN.md` として書きだす。形式は `references/plan-template.md` に従う。

書きだしたら、着手前にユーザーへ提示する。伝えるのは次の 4 点に絞る。

- 検出した臭いの件数と、優先度別の内訳
- 最初に着手する 3 項目とその理由
- テストで保護されていない範囲の広さ（保護コストの見積もり）
- 棚上げを推奨する項目（あれば理由とともに）

**着手順の原則：優先度が高いものからではなく、「保護しやすく効果が大きいもの」から。** 優先度 High の項目が巨大な未テスト領域の中心にある場合、まず周辺の小さな項目から手をつけてテストの足場を広げるほうが速い。この判断理由は計画に書き残す。

ユーザーが範囲を絞ったり順序を変えたりしたら、計画ファイルを更新してから着手する。

## フェーズ 3：1 項目ずつ修正する

`references/refactoring-loop.md` を読んでから実行する。ここには 3 ステップ（保護 → 変更 → 検証）の詳細、テストを変えてはいけない理由、差し戻しの判断基準がある。

各項目について 3 ステップを順に踏む。**3 つを同じ文脈で続けてやらない。**

| ステップ | やること | 指示文 | 層 1 | 層 2 |
|---|---|---|---|---|
| A | テストで保護し、グリーンを確認してコミット | 「テストで保護する」 | サブタスク① | セッション① |
| B | 小さなステップで書きかえ、各ステップでグリーン維持 | 「書きかえる」 | サブタスク② | セッション② |
| C | グリーンとふるまい不変を独立に検証 | 「検証する」 | サブタスク③ | セッション③ |

**A と B を分ける理由。** テストを書いた本人が実装を変えると、自分の変更に合わせてテストを緩めてしまう。分離することでテストが独立した基準として機能する。

**C を省かない理由。** B は「グリーンになった」と報告するが、テストが消されていたり skip が増えていたりすることがある。C はテスト件数と assertion の差分を機械的に突きあわせる。層 2 では**必ず新しいセッションで** C を行う。同じ文脈のまま検証すると、自分の変更を「正しいはず」という前提で見てしまう。

ステップの境界で司令役に戻る。そこで `REFACTORING_PLAN.md` の状態を更新し、次へ進むか差し戻すかを判断する。同じ項目で 3 回やりなおして通らなければ棚上げにする。粘るより他の項目に時間を使ったほうが総合的な成果が大きい。

**3 項目ごとに、ユーザーへ短い中間報告を入れる。** 長時間黙って進めると、方針がずれていた場合の手戻りが大きい。

## テストがグリーンでも、動くとは限らない

単体テストが守るのは**個々の関数のふるまい**だ。「アプリケーションが起動するか」「画面が正しく出るか」「実際の入出力で動くか」は別の問題で、テストの外にある。

だから、**可能なら一度は実際に動かす。** テストがグリーンでも、次のような壊れ方はテストに映らない。

- ビルド設定への追加漏れ（新しいファイルを作ったが `Makefile` に足していない）
- 初期化順序の変更（テストでは個別に呼ぶので気づかない）
- 外部ライブラリとの結合部分（テストでは代役に差しかえている箇所）
- リンク順序やシンボルの衝突

**逆も成り立つ。動いてもテストがなければ、壊したかどうかは分からない。** 手で動かして「大丈夫そう」と思うのは確認ではない。両方が必要で、役割が違う。

実行できない環境（依存が入っていない、対話的な操作が必要）なら、そのことを報告に明記する。「テストはグリーンだが実動作は未確認」と書けば、ユーザーが自分で確かめる判断ができる。

## フェーズ 4：仕上げ

全項目、あるいは合意した範囲が終わったら次を行う。

1. **仕様を起こす。** 追加したテストは「動かして確かめられる仕様」になっている。テスト名と assertion を自然言語にまとめなおし、対象モジュールの仕様メモとして残す。指示文は `references/task-prompts.md` の「仕様起こし」を使う。
2. **不自然な点を報告する。** 仕様を起こす過程で見つかった、実装の抜け・漏れ・不自然なふるまいを列挙する。これらは**このリファクタリングでは直さない**（ふるまいの変更になるため）。バグ候補としてユーザーに報告し、別作業として扱う。
3. **最終報告。** 修正した項目、棚上げした項目とその理由、追加したテストの件数、新たに見つかったバグ候補を報告する。棚上げを黙って省略しない。範囲を縮めるかどうかはユーザーの判断だ。

## コンテキストを守る

長い作業なので、文脈は必ず限界に近づく。次を守ると、圧縮やセッション切り替えが起きても作業を続けられる。

- **実装ファイルを自分で開かない。** 読む必要が生じたら、その理由を明確にしてから作業役に任せる（層 1）か、区切ったセッションで読む（層 2）。判断に必要なのは通常 5 行以内の答えで、ファイル全文ではない。
- **返答量を必ず制限する。** すべての指示文に `task-prompts.md` の「共通ルール：返答量の制限」を含める。これを省くと長いコードや差分が文脈に流れこみ、分離した意味がなくなる。
- **テスト出力の全文を受けとらない。** 必要なのは「件数と pass/fail/skip の数」だけ。失敗したときだけ、失敗したテスト名と 1 行のメッセージを聞く。
- **状態は会話ではなくファイルに書く。** `REFACTORING_PLAN.md` が唯一の正本。1 項目終わるたびに更新する。ためると、圧縮が起きたときに進捗が失われる。
- **圧縮やセッション切り替えが起きたら、まず計画ファイルを読みなおす。** 前提（テストコマンド、着手前の件数）と台帳の状態を回復してから作業を続ける。記憶に頼って再開しない。

### 書きだす作業も小分けにする

入力だけでなく**出力にも上限がある**。1 回の応答で書ける量には限りがあり、超えると書きこみそのものが失敗する。とくに `REFACTORING_PLAN.md` は台帳・棚上げ・バグ候補・作業ログを含むので、全体を一度に書こうとすると詰まりやすい。

だから**節ごとに分けて書く**。

1. まず「前提」と「検出結果の内訳」だけでファイルを作る
2. 次に P1 の台帳を追記する
3. 次に P2 / P3 / 棚上げを追記する
4. 最後にバグ候補と作業ログを追記する

各段階が独立した追記なので、途中で失敗しても前の段階は残る。長い表を書くときは「1 回の追記で 15 行まで」を目安にする。

同じことは報告にも当てはまる。**ユーザーへの中間報告は、そのつど短く出す。** 全項目が終わってから長大な総括を書こうとすると、書ききれずに失敗する。項目 3 つごとに数行ずつ出すほうが、ユーザーにとっても追いやすい。

## やらないこと

- **ふるまいを変えない。** リファクタリング中にバグを見つけても直さない。記録してユーザーに報告する。「ついで」の修正は、テストがグリーンなのに動作が変わるという最悪の状態を招く。
- **テストを都合よく書きかえない。** テストが落ちたら実装を戻す。テストの期待値を変えて通すのは、保護を自分で外す行為だ。
- **一度に複数の臭いを直さない。** 1 ステップ 1 変更。レッドになったとき原因を特定できる粒度を保つ。
- **網羅を目指さない。** 引数の組みあわせはほぼ無限にある。「テストが皆無」から「一部でもある」状態にするのが目的。書ききれない量を目指すと途中で破綻する。
