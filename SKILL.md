---
name: code-smell-refactoring
description: コードの臭い（重複、長すぎる関数、肥大化した型、責務の誤配置、不明瞭な名前、複雑な条件分岐、強すぎる依存、データの散在、不要なコード、理解しづらいロジック）を検出して優先度づけし、テストで保護しながら小さなステップでリファクタリングする。「リファクタリングして」「コードの臭いを分析して」「レガシーコードを改善したい」「テストがないコードを直したい」「技術的負債を返済したい」「この関数が長すぎる／読みにくいので整理して」といった依頼で必ず使う。特定の手法名（メソッド抽出、ガード節導入、依存逆転など）を挙げられた場合、テストのないコードに単体テストを追加する場合、既存の実装を壊さずに設計を変えたい場合にも使う。言語は問わない。
---

# コードの臭いにもとづく段階的リファクタリング

中核は 3 点。

1. **テストのないコードは触らない。** まず保護し、そのあとで書きかえる。
2. **小さなステップでグリーンを保つ。** 各ステップ後にテストを実行し、レッドになったら即座に戻す。
3. **テストは変えない。** リファクタリングとは外部から見たふるまいを変えずに内部構造を改善すること。テストを書きかえたくなったら、それは仕様変更。

このスキルは**言語中立**。言語固有の書きかたと落とし穴は `references/lang/` にあり、対象言語のパックだけを読む。

## 着手前に決める 2 つのこと

**1. 実行方式。** `references/capabilities.md` を読む。サブタスクを起動できるか（層 1）／単一の会話のみか（層 2）で進め方が変わる。**判断がつかなければ層 2 として振るまう**（層 2 の手順は層 1 でも正しく動く）。

**2. 言語パック。** 対象の主要言語を確かめ、該当パックがあれば以後の作業役に読ませる。

| 対象 | パック |
|---|---|
| C / C++ | `references/lang/c.md` |
| Java / C# / Kotlin ほかクラスベース OO | `references/lang/oo.md` |
| Python / JavaScript / TypeScript / Ruby ほか動的言語 | `references/lang/dynamic.md` |
| 上記以外 | パックなしで進める。コアだけで足りる |

パックが無い言語でも作業は成立する。**足りないと感じた点は最後に報告する**（このスキルを育てる材料になる）。

## 共通の原則：作業役と司令役を分ける

どちらの層でも、**司令役は実装コードをほとんど読まない。** 長いソースを読むと文脈が埋まり、方針や進捗を見失う。司令役は台帳の管理と判断に徹し、コードを読む・テストを書く・修正する作業は作業役が担う。層 1 では作業役が別のサブタスク、層 2 では**文脈を切ったあとの自分**。

司令役が保持するのは、対象のファイル一覧とテスト実行コマンド、臭いの台帳と優先度、各項目の状態、作業役から返った要約（各 20 行以内）だけ。

進捗台帳は会話ではなくファイル（`REFACTORING_PLAN.md`）に置く。**このファイルが唯一の正本。** 会話が圧縮されてもセッションが変わっても、読みなおせば再開できる。

## 手順

| フェーズ | 内容 | 詳細 |
|---|---|---|
| 0 | 環境の確認（テストが動くか） | 下記 |
| 1 | 臭いの検出と優先度づけ | `references/smells.md` |
| 2 | 計画の記録と合意 | `references/plan-template.md` |
| 3 | 1 項目ずつ修正（保護 → 変更 → 検証） | `references/refactoring-loop.md` |
| 4 | 仕上げ（仕様の起こしなおし、報告） | 下記 |

各ステップの指示文は `references/task-prompts.md`。層 1 ではサブタスクへ渡す文、層 2 では自分が守る規則として読む。フェーズ 1 と 3 に入る前に開く。

## フェーズ 0：足場をつくる

テストが動かない環境で始めると、失敗の原因が「自分の変更」か「環境」か切り分けられない。

1. **テスト実行コマンドを特定する。** 以後このコマンドを全ステップで使う。
2. **現状のグリーンを確認する。** **すでにレッドなら、そこで止まってユーザーに報告する。**
3. **テストが無い場合は、フレームワークの疎通を確認する。** 空のテストで OK を得たあと、**必ず失敗する assertion を入れてレッドが出ることを確認**し、確認後に削除する。この確認を省くと、テストが 1 件も収集されていないのに「グリーン」と誤認する。

`scripts/verify_harness.sh` が言語を判定してコマンド候補を出す。このフェーズは司令役が自分で実行してよい（出力が短く、全作業の前提になる）。

## フェーズ 2：計画を記録して合意する

形式は `references/plan-template.md`。書きだしたら着手前にユーザーへ提示する。伝えるのは 4 点だけ——検出件数と優先度別の内訳／最初に着手する 3 項目とその理由／テストで保護されていない範囲の広さ／棚上げを推奨する項目。

**着手順の原則：優先度が高いものからではなく、「保護しやすく効果が大きいもの」から。** 優先度 High の項目が巨大な未テスト領域の中心にあるなら、まず周辺の小さな項目でテストの足場を広げるほうが速い。この判断理由は計画に書き残す。

## フェーズ 3：1 項目ずつ修正する

`references/refactoring-loop.md` を読んでから実行する。各項目で 3 ステップを順に踏む。**3 つを同じ文脈で続けてやらない。**

| ステップ | やること | 層 1 | 層 2 |
|---|---|---|---|
| A | テストで保護し、グリーンを確認してコミット | サブタスク① | セッション① |
| B | 小さなステップで書きかえ、各ステップでグリーン維持 | サブタスク② | セッション② |
| C | グリーンとふるまい不変を独立に検証 | サブタスク③ | セッション③ |

**A と B を分ける理由。** テストを書いた本人が実装を変えると、自分の変更に合わせてテストを緩めてしまう。分離するとテストが独立した基準として機能する。

**C を省かない理由。** B は「グリーンになった」と報告するが、テストが消えていたり skip が増えていたりする。C は件数と assertion を機械的に突きあわせる。層 2 では**必ず新しいセッションで**行う。

ステップの境界で司令役に戻り、台帳を更新して次へ進むか差し戻すかを判断する。**同じ項目で 3 回やりなおして通らなければ棚上げ。** 粘るより他の項目に時間を使うほうが総合的な成果が大きい。**3 項目ごとにユーザーへ短い中間報告を入れる。**

## テストがグリーンでも、動くとは限らない

単体テストが守るのは**個々の関数のふるまい**だけだ。「起動するか」「実際の入出力で動くか」はテストの外にある。だから**可能なら一度は実際に動かす。** 次の壊れ方はテストに映らない。

- ビルド定義への追加漏れ（新しいファイルを作ったが登録していない）
- 初期化順序の変更、リンク順序やシンボルの衝突
- 外部ライブラリとの結合部分（テストでは代役に差しかえている箇所）

**逆も成り立つ。動いてもテストがなければ、壊したかどうかは分からない。** 手で動かして「大丈夫そう」と思うのは確認ではない。両方が必要で、役割が違う。実行できない環境なら「テストはグリーンだが実動作は未確認」と報告に明記する。

## フェーズ 4：仕上げ

1. **仕様を起こす。** 追加したテストは「動かして確かめられる仕様」になっている。テスト名と assertion を自然言語にまとめなおす（指示文は `task-prompts.md`「仕様起こし」）。
2. **不自然な点を報告する。** 実装の抜け・漏れ・不自然なふるまいは**このリファクタリングでは直さない**（ふるまいの変更になる）。バグ候補として報告し、別作業として扱う。
3. **最終報告。** 修正した項目、棚上げした項目とその理由、追加したテスト件数、新たなバグ候補。**棚上げを黙って省略しない。** 範囲を縮めるかはユーザーの判断だ。
4. **作業が続くなら引きつぎ資料を書く。** 台帳は「何をするか」を持つが「どう進めるか」は持たない。`plan-template.md`「引きつぎ資料」を参照。

## コンテキストと出力量を守る

- **実装ファイルを自分で開かない。** 判断に必要なのは通常 5 行以内の答えで、ファイル全文ではない。
- **返答量を必ず制限する。** すべての指示文に `task-prompts.md`「共通ルール：返答量の制限」を含める。省くと分離した意味がなくなる。
- **テスト出力の全文を受けとらない。** 「件数と pass/fail/skip」だけ。失敗時のみテスト名と 1 行のメッセージ。
- **状態は会話ではなくファイルに書く。** 1 項目終わるたびに更新する。ためると圧縮時に失われる。
- **圧縮やセッション切り替えが起きたら、まず計画ファイルを読みなおす。** 記憶に頼って再開しない。
- **書きだしも小分けにする。** 出力にも上限がある。長いファイルや長い報告を 1 回で書こうとすると、書きこみ自体が失敗する。詳細は `capabilities.md`「出力量の制限」。

## やらないこと

- **ふるまいを変えない。** バグを見つけても直さず記録して報告する。「ついで」の修正は、テストがグリーンなのに動作が変わるという最悪の状態を招く。
- **テストを都合よく書きかえない。** 落ちたら実装を戻す。期待値を変えて通すのは保護を自分で外す行為。
- **一度に複数の臭いを直さない。** 1 ステップ 1 変更。レッドの原因を特定できる粒度を保つ。
- **網羅を目指さない。** 「テストが皆無」から「一部でもある」状態にするのが目的。書ききれない量を目指すと途中で破綻する。
